[workspace]
members = ["server", "desktop/src-tauri"]
resolver = "2"

[workspace.package]
version = "0.3.3"
edition = "2024"
license = "MIT"

[patch.crates-io]
# scap 0.1.0-beta.1 was published with two stale windows-capture 1.5 API calls.
# Keep the patch local until upstream publishes a corrected release.
scap = { path = "third_party/scap" }
